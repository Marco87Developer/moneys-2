name: moneys
description: A set of classes useful for managing money, for save in a database and retrieve from it.
version: 2.2.0
repository: https://github.com/Marco87Developer/moneys
homepage: https://marco87.dev

environment:
  sdk: ">=2.13.3 <3.0.0"
  flutter: ">=2.2.2"

dependencies:
  flutter:
    sdk: flutter

  geos: ^1.3.1
  google_maps_flutter: ^2.0.6
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^1.0.1

flutter:
